namespace Adapter; 

// intent to convert the interface of a class into another interface
// there's some method from external interface that needs to be converted (adapted) is called adaptee
// the method that is adapted to is called target. Target defines the domain specific interface that the client uses
// adapter is the wrapper that adapts the adaptee to the target

// there are two types of adapter patter
// -- object adapter pattern
// -- class adapber pattern